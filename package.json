{
  "name": "newpaperwallet",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild app.js --bundle --format=iife --minify --outfile=bundle.js",
    "serve": "python3 -m http.server 8080",
    "start": "npm run build && npm run serve",
    "build-stealth": "esbuild stealth-app.js --bundle --format=iife --minify --outfile=stealth-bundle.js",
    "build-all": "npm run build && npm run build-stealth"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@noble/hashes": "^1.8.0",
    "@noble/secp256k1": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
